@layout('layouts/main')
@set('fullWidthLayout', true)
@set('currentPage', `/${user.username}`)

@section('main')
<div class="w-full">
  <div class="w-full grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
    @each(poem in poems)
    @!component('components/poem-card', {poem})
    @end
  </div>
</div>
@end